server:
  port: 8083

spring:
  application:
    name: pagos-service
  data:
    mongodb:
      uri: mongodb://root:rootpassword@localhost:27017/micro_db?authSource=admin

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
